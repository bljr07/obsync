# Obsync Frontend

Vite + React dashboard for observability and configuration.

## Development
- `npm install`
- `npm run dev`

Configure the server URL and vault ID, then sign in with the bootstrap
credentials printed by the backend on first run.
